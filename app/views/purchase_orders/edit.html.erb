<p><%= link_to "← Back to Job", job_path(@job), class: "btn" %></p>

<h1>Edit Purchase Order <%= @purchase_order.po_number %></h1>

<%= form_with model: [@job, @purchase_order], local: true do |f| %>
  <% if @purchase_order.errors.any? %>
    <div class="alert">
      <h2><%= pluralize(@purchase_order.errors.count, "error") %> prohibited this from being saved:</h2>
      <ul>
        <% @purchase_order.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div style="margin-bottom: 1rem;">
    <%= f.label :supplier_name, "Supplier Name" %><br>
    <%= f.select :supplier_name, options_for_select([["Builders Warehouse", "Builders Warehouse"], ["Plumbworld", "Plumbworld"], ["Mr Price Plumbing", "Mr Price Plumbing"], ["Buco", "Buco"], ["Cashbuild", "Cashbuild"], ["Other", "Other"]], @purchase_order.supplier_name), { include_blank: "Select a supplier" }, required: true, style: "padding: 0.5rem; border: 1px solid var(--border); border-radius: 6px; font-size: 0.9rem; width: 100%; max-width: 400px;" %>
  </div>

  <div style="margin-bottom: 1rem;">
    <%= f.label :order_date, "Date Bought" %><br>
    <%= f.date_field :order_date, required: true, style: "padding: 0.5rem; border: 1px solid var(--border); border-radius: 6px; font-size: 0.9rem;" %>
  </div>

  <div style="margin-bottom: 1rem;">
    <%= f.label :notes %><br>
    <%= f.text_area :notes, placeholder: "Any additional notes..." %>
  </div>

  <h3>Line Items</h3>
  <table style="width: 100%; margin-bottom: 1rem; border-collapse: collapse;">
    <thead>
      <tr>
        <th style="text-align: left; padding: 0.75rem; border-bottom: 2px solid var(--border);">Material / Description</th>
        <th style="text-align: left; padding: 0.75rem; border-bottom: 2px solid var(--border);">Quantity</th>
        <th style="text-align: left; padding: 0.75rem; border-bottom: 2px solid var(--border);">Unit Price (R)</th>
        <th style="text-align: left; padding: 0.75rem; border-bottom: 2px solid var(--border);">Total (R)</th>
      </tr>
    </thead>
    <tbody id="items">
      <%= f.fields_for :items do |item_form| %>
        <tr class="item-row">
          <td style="padding: 0.75rem; border-bottom: 1px solid var(--border);"><%= item_form.text_field :description, style: "width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 6px;" %></td>
          <td style="padding: 0.75rem; border-bottom: 1px solid var(--border);"><%= item_form.number_field :quantity, min: 1, step: 1, class: "qty", style: "width: 100px; padding: 0.5rem; border: 1px solid var(--border); border-radius: 6px;" %></td>
          <td style="padding: 0.75rem; border-bottom: 1px solid var(--border);"><%= item_form.number_field :unit_price, min: 0, step: 0.01, class: "price", style: "width: 120px; padding: 0.5rem; border: 1px solid var(--border); border-radius: 6px;" %></td>
          <td class="line-total" style="padding: 0.75rem; border-bottom: 1px solid var(--border); font-weight: 600;">R <%= number_with_precision((item_form.object.quantity || 0) * (item_form.object.unit_price || 0), precision: 2) %></td>
        </tr>
      <% end %>
    </tbody>
    <tfoot>
      <tr>
        <td colspan="3" style="text-align: right; padding: 0.75rem; font-weight: 600; color: var(--text-muted);">Subtotal (excl. VAT):</td>
        <td id="subtotal" style="padding: 0.75rem; font-weight: 600; color: var(--text-muted);">R <%= number_with_precision(@purchase_order.subtotal, precision: 2) %></td>
      </tr>
      <tr>
        <td colspan="3" style="text-align: right; padding: 0.75rem; font-weight: 600; color: var(--text-muted);">VAT (15%):</td>
        <td id="vat-amount" style="padding: 0.75rem; font-weight: 600; color: var(--text-muted);">R <%= number_with_precision(@purchase_order.vat_amount, precision: 2) %></td>
      </tr>
      <tr>
        <td colspan="3" style="text-align: right; padding: 0.75rem; font-weight: 700; font-size: 1.05rem; border-top: 2px solid var(--border);">Total (incl. VAT):</td>
        <td id="grand-total" style="padding: 0.75rem; font-weight: 700; font-size: 1.05rem; border-top: 2px solid var(--border);">R <%= number_with_precision(@purchase_order.total_including_vat, precision: 2) %></td>
      </tr>
    </tfoot>
  </table>

  <div style="margin-bottom: 1rem;">
    <%= f.submit "Update Purchase Order", class: "btn" %>
  </div>
<% end %>

<script>
function recalc() {
  let subtotal = 0;
  document.querySelectorAll('.item-row').forEach(row => {
    const qty = parseFloat(row.querySelector('.qty').value) || 0;
    const price = parseFloat(row.querySelector('.price').value) || 0;
    const lineTotal = qty * price;
    row.querySelector('.line-total').textContent = 'R ' + lineTotal.toFixed(2);
    subtotal += lineTotal;
  });
  const vatRate = 15.0;
  const vatAmount = subtotal * (vatRate / 100.0);
  const total = subtotal + vatAmount;
  document.getElementById('subtotal').textContent = 'R ' + subtotal.toFixed(2);
  document.getElementById('vat-amount').textContent = 'R ' + vatAmount.toFixed(2);
  document.getElementById('grand-total').textContent = 'R ' + total.toFixed(2);
}
document.querySelectorAll('.qty, .price').forEach(input => input.addEventListener('input', recalc));
recalc();
</script>
